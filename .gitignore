# 🔴 部署脚本里有服务器明文密码（HOST/USER/PWD），绝不进仓——私有仓也不行
_deploy_lifestamps.py
_deploy_*.py

# 字体源文件（48MB，子集化用的原始 ttf/otf），仓里只留 app/fonts 下已子集化的 woff2
_fonts_src/

# 录屏、样张、比稿图、PRD —— 都是二进制过程物料，不是代码
*.mp4
*.docx
*.png
生活图鉴样张/
xhs-package/

# 本地杂项
.DS_Store
Thumbs.db

# ---- Capacitor（8-27 加）----
# 🔴 原生工程和依赖一律不进仓：它们由 CI 用 `npx cap add ios` 每次现生成。
#    好处=本地零安装、仓库小；代价=任何手改原生工程的东西会被下一次生成冲掉，
#    所以图标、Info.plist 改动必须放在 ios-assets/ 里由 workflow 注入，别直接改 ios/。
node_modules/
ios/
android/
package-lock.json

# 图标是要进仓的（上面 *.png 一刀切会把它也挡掉）
!ios-assets/**/*.png
!android-assets/**/*.png

# 🔴 内部规划文档不进仓（2026-08-28 已从全历史抹掉后加的这条）：
#    里面有定价/内购档位/未上线功能清单，仓库要临时公开跑 CI，这些不能跟着出去。
生活图鉴-实施规格-*.md
生活图鉴-当前实现实况-*.md
生活图鉴-计划-*.md
生活图鉴-首屏重设计-*.md
生活图鉴V*.md

# ---- 后端（8-28 加）----
# 运行时产生的库：分享内容 + 匿名赠礼，不进仓
server/data/
__pycache__/

# 安卓签名 keystore + 口令卡（9-02）：绝不进仓，备份在 D:\ios密钥备份\lifestamps-android\
_android_signing/
