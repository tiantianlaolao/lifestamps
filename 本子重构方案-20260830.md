# 本子重构方案（2026-08-30 用户拍板「一次性做」）

## 模型（用户主导的 v2）

- **本子 = 一年一本**：书架退役，书内 12 个月份便签（常在，没到的月空白）→ 月历 → 点某天日页就地展开
- **日页组件全 App 唯一**（今日页那套，含托盘/卷角/分享）：
  - 今日 tab = 这本书自动摊开在今天，卷角往回翻**不设限**（🔴用户点名不能变）
  - 本子里点某天 = 同一组件，tab 高亮留在「本子」，顶部给「回到本子」——补盖不再把人拽出书
- **那行字只有一个槽位**：判词 = 系统代笔的默认值；点那行字原地改，改完是你的话，擦了判词回来；
  「补一句 / 改一改这天的话」独立按钮退场；没字时给淡淡的「写一句…」占位（未来的日子没有）
- **往年**：攒够第二年才出现年份切换（渐进披露）
- 🔴 新红线：**本子本体永远免费**，新年换本子只卖封面/纸样（付费永不碰记录能力的延伸）

## 实现要点

- 日页进本子 = **bookMode**：激活 #page-today 那份 DOM（全 App 只有一份日页 DOM，绝不复制 id），
  但 tab 高亮指「本子」；回到月历 = 关 bookMode 回 #page-memories。
  🔴 这是"能滚就抢手势"雷的解法：日页永远是那份不滚的 DOM，月历滚它自己的
- 翻页视图（renderFlipView/openFlip/flip-note）退役；月历/周历点行 → bookMode；dayPanel 退役（日页一步可达，面板失去价值）
- 判词/写话编辑：点行 → 原地换 input（保存挂 blur+幂等，🔴 iOS 键盘弹着时禁 click 的老坑）
- 断言页 _verdictnote 改判据；test12 里涉书架/翻页的断言跟着改

## 分工位（当天串行做完）

P1 那行字统一 → P2 年册+月便签 → P3 bookMode+翻页视图退役 → 全量回归+三语截图
